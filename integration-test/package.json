{
  "name": "integration-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:strip && npm run test:mode-test",
    "test:strip": "vite build && node verify.js strip",
    "test:mode-test": "vite build --mode test --outDir dist-mode-test && node verify.js mode-test"
  },
  "dependencies": {
    "@types/node": "^24",
    "@types/react": "^19",
    "@vitejs/plugin-react": "^6.0.1",
    "oxc-remove-attributes": "portal:..",
    "react": "^19",
    "react-dom": "^19",
    "typescript": "^5.8",
    "vite": "^8.0.0"
  }
}
